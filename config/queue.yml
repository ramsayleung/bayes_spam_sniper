default: &default
  dispatchers:
    - polling_interval: 1
      batch_size: 500
  workers:
    - queues: "*"
      threads: 2
      processes: <%= ENV.fetch("JOB_CONCURRENCY", 1) %>
      polling_interval: 0.1

development:
  dispatchers:
    - polling_interval: 1
      batch_size: 500
  workers:
    - queues: "spam_analysis"
      processes: 1
      threads: 1
    - queues: "training"
      processes: 1
      threads: 1
    - queues: "batching"
      processes: 1
      threads: 1
    - queues: "low_priority"
      processes: 1
      threads: 1
    - queues: "*"
      processes: <%= [ENV.fetch("JOB_CONCURRENCY", "1").to_i - 5, 1].max %>
      threads: 1

test:
  <<: *default

production:
  dispatchers:
    - polling_interval: 1
      batch_size: 500
  workers:
    - queues: "spam_analysis"
      processes: 1
      threads: 2
    - queues: "training"
      processes: 1
      threads: 2
    - queues: "batching"
      processes: 1
      threads: 2
    - queues: "low_priority"
      processes: 1
      threads: 2
    - queues: "*"
      processes: <%= [ENV.fetch("JOB_CONCURRENCY", "1").to_i - 5, 1].max %>
      threads: 2
