global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  # - "first_rules.yml"

scrape_configs:
  - job_name: 'telegram_bot'
    static_configs:
      # In Kamal's internal network, the web service would be accessible as 'telegram_spam_sniper_bot-web-1' or similar
      # For the actual IP address approach:
      - targets: ['telegram-web:3000']
    scrape_interval: 5s
    # Add authentication headers if needed
    # headers:
    #   Authorization: Bearer your-token-here
